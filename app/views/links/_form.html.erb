<div class="container">
<%= form_with(model: link, local: true) do |form| %>
  <% if link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
</div>

 <div class="container">
     <%= form.text_field :title, id: :link_title, class:"form-control" ,placeholder: "Enter Title"%><br>
     <%= form.text_field :url, id: :link_url, class:"form-control", placeholder: "Enter URL" %><br>
     <%= form.submit %>
 <% end %>
 </div>
